// Phase 3: friends list with last message + online dot, GET /friends + GET /conversations.
export default function ConnectionsPage() {
  return (
    <div className="min-h-screen flex items-center justify-center p-6 text-center">
      <div>
        <h1 className="text-2xl mb-2">Connections</h1>
        <p className="text-muted-foreground text-sm">Phase 3 — see VENTLY_PLAN.md §6.</p>
      </div>
    </div>
  );
}
