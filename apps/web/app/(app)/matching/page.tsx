// Phase 2: emits match:join socket event; waits for match:found.
export default function MatchingPage() {
  return (
    <div className="min-h-screen flex items-center justify-center p-6 text-center">
      <div>
        <h1 className="text-2xl mb-2">Matching</h1>
        <p className="text-muted-foreground text-sm">Phase 2 — see VENTLY_PLAN.md §6.</p>
      </div>
    </div>
  );
}
